    int maxAttempts = 3;
    
    // looping n (maxAttempts) times
    for(int i = 0; i < maxAttempts; i++)
    {
    	// get input and check it
    }
    
    // here you would maybe want to send the user
    // into a loop of nothing.
    
    while(true)
    {
        // show a message or whatever
        Console.ReadKey();
    }
