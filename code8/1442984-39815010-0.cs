    enum Player
    {
        FIRST,
        SECOND
    }
    static class PlayerMethods
    {
        public static String swap(this Player s)
        {
            switch (s1)
            {
                case Player.FIRST:
                    return Player.SECOND;
                case Player.SECOND:
                    return Player.FIRST;            
            }
        }
    }
