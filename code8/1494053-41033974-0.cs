    object value = true;
           if(value.GetType()==typeof(string))
           {
               //Textbox
           }
           else if(value.GetType()==typeof(bool))
           {
               //checkbox
           }
