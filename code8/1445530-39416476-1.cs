    for(int i = 0; i < row.ItemArray.Length; i ++)
    {
        if( d.length == 0)
        {
            d = item.ToString();
        }
        else
        {
            d = d + "|" + item.ToString();
        }
    }
