    List<int> array = new List<int>();
    array.Add(2);
    array.Add(3);
    array.Add(4);
    for (int z = 0; z < array.Length; z++)
    {
        array[z] = array[z]-1;
    }
    Console.ReadLine();
