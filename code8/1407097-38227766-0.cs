    for (int i = 0; i < colour.Length; i++) {
                        if (colour[i] == MyColours.Blue.ToString()) {
                            return true;
                    }
                    else if(colour[i] == MyColours.Red.ToString()){
                            return true;
                    }
                        else if (colour[i] == MyColours.Orange.ToString()) {
                            return true;
                        }
    }
