        public void MySave(IRibbonControl control, bool cancelDefault)
        {
            Application.ActiveDocument.Save();
        }
        public void MySaveAs(IRibbonControl control, bool cancelDefault)
        {
            // TODO: Ask the user to provide a file name and call
            // Application.ActiveDocument.SaveAs passing in the provided name
        }
