    if (DateTime.Now.DayOfWeek == DayOfWeek.Friday
        || (DateTime.Now >= StartPromoPrice && DateTime.Now < EndPromoPrice))
    {
        //get promo price
    }
    
    else
    {
        //use standard price
    }
