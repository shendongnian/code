    foreach(item in list)
    {
        try
        {
             //code that add item to table 
        }
        catch(Exception ex)
        {
            Console.WriteLine(ex.Message);
        }
    }
