    foreach (var e in results )
     { 
     e.values,
     e.values
      }
     //3rd approach
     var db = new DbContext();
      // automatic  inner join
      db.Suppliers.Include(a => a.Guards); 
