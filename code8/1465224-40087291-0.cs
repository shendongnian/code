    root root = new root();
    rootHeader header = new rootHeader() { 
       SystemName = "pontaz", 
       SystemVersion = 1, 
       SessionId = 1, 
       CommandName = "Connect", 
       ConnectOption = 0 
    };
    // add rootHeader to root element somewhere here
    // before serializing it...
