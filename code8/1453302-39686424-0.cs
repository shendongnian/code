    quicksort(enemies);
    for(int = 0; i < players.Count; i++)
    {
        for(int j = 0 < enemies.Count; j++)
        {
            if(players[i].AggroThreshhold < enemies.Aggro)
            {
                 break;
            }
            // Perform what happens when enemy is high on aggro threshold.
        }
    }
