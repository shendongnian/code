    public class Output 
    {
        string aProperty;
        int Count;
        public override string ToString()
        {
            return "I am an Output with aProperty: " + aProperty + " and Count " + Count;
        }
    }
