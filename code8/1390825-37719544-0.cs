    public AuthorCollector(int _startYear, int _endYear)
    {
       _con = _cs.GetConnection();
       initialize(_startYear, _endYear);
    }
    private void initialize(int startYear, int endYear) {
        // Do initalize
    }
