    List<string> strListNew = new List<string>();
    foeach(var item in strList){
      if (item(inputStr.Contains))
      {
        strListNew.Add(""); 
      }
      else{
        strListNew.Add(item);
      } 
    }
