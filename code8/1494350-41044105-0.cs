    public string FlatenSummaryRows(IEnumerable<SymmaryRow> summaryRows) {
        //here make your collection flat
        string result = string.Empty;
        for each (var item in  summaryRows) {
           result += $"this is my item {prop1}";
        }
    }
