    for (int i = 0; i < page.Request.Form.Keys.Count; i++)
            {
                if (page.Request.Form.Keys[i].Contains("GelleryImagesPager"))
                {
                   ...
                    break;
                }
            }
