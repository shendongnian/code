     var list = new List<AppSettings>();
            if (list.Any(x => x.Name == NameEnteredByUser))
            {
                //name exists
            }
            else
            {
                //no such name used before
            }
