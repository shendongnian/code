    HashSet<Int16> evenNumbers = new HashSet<Int16>();
        for (Int16 i = 0; i < 20; i++)
        {
            evenNumbers.Add(i);
        }
       
        if (evenNumbers.Contains(0))
        {
           /////
        }
