            for (int i = 0; i < Demo.Length; i++)
            {
                switch (Demo[i].ToLower())
                {
                    case"dbk":
                        DBK.Style.Add("display", "table-row");
                        break;
                        //And so on
                    default:
                        break;
                }
            }
