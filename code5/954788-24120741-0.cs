            public createGroups()
            {
                InitializeComponent();
                if(groupbtn!== null)
                groupbtn = new ObservableCollection<Group>();
            }
    public static ObservableCollection<Group> groupbtn;
