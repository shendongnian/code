            foreach (String item in SelectedColumns)
            {
                if (item == field.Text)
                {
                    SelectedColumns.Remove(item);
                }
            }
