    private void btnPoisson_Click(object sender, EventArgs e)
    {
        string imageLocation = ""; // Represents the path of the image file.
        pictureBox1.Image = Image.FromFile(imageLocation);
    }
    
