    List<string> notInanswerList = new List<string>();
    foreach(textbox t in Panel1.Controls.OfType <TextBox>()))
    {
     choiceList.Add(t.Text.Trim());
     if(!answerList.Contain(s.Text.Trim()
    {
     notInanswerList.Items.Add(t.Text.Trim());
    }
    }
