     foreach( var v in SharedToDomains.Values )
     {
           string val = v.ToString();
           // Or, not sure if you wanted to write to the 
           // console but if so:
           System.Console.WriteLine(v.ToString());
     }
