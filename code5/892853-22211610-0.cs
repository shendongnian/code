    private void MainForm_KeyDown(object sender, KeyEventArgs e)
            {
    
                switch (e.KeyCode)
                {
         case Keys.G:
                            if (e.Modifiers == Keys.Control)
                            {
                  //Do something
                            }
                                break;
                 }
             }
