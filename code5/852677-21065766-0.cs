    if (tb.Text.Length > 0)
    {
       if(char.IsDigit(tb.Text[tb.Text.Length - 1]) count = 0;
       else 
       { 
           dotControl = false;
           count = 0;
       }
        tb.Text = tb.Text.Remove(tb.Text.Length - 1, 1);
    }
