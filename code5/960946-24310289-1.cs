            try
            {
               // lots of code emitted
                if (feed.Items != null && feed.Items.Count > 0)
                {
                    // lots more code emitted 
                   
                    return feedData;
                }
                else
                {
                    return null;
                }
            }
