    public ClassA
    {
        private ClassB classBInstance;
        void buttonClick(...)
        {
            var someVariable = classBInstance.GetVariable();
            SomeFunction(someVariable);
        }
    }
