    private static void UpdateList(List<int> list)
    {
                    //list = new List<int>(); remove that
                    list.Add(1);
                    list.Add(2);
                    Console.WriteLine(list.Count); // prints 2 
    }
