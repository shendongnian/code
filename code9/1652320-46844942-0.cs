    protected void btn_Click(object sender, EventArgs e)
        {
         insert();
         View();
         }
        private void insert()
        {
          // write insert query 
        }
       private void View()
        {
         // write select query and bind to grid
        }
