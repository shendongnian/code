    List<Worker> workers;
    public void PrintWages()
    {
        foreach (var worker in workers)
        {
            switch (worker)
            {
                 case Worker w: 
                 // w will be of type Worker
                 break;
            }
        }
    }
