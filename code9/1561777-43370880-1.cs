    static void Main(string[] args)
        {
            string s = "Test";
             for (int i = 0; i < s.Length; i++)
                {
                    //Convert every leter from the string in ASCII value.
                    int value = s[i];
                    Console.WriteLine(value);
                }
        }
