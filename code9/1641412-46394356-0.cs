     public int size()
    {
         Element tmp = first;
         int cnt =0;
         while(temp != null)
         {
             cnt++;
             temp = temp.next
         }
          return cnt;
    }
