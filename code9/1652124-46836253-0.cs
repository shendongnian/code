     if (newGOCenter.y <= 1)
                        {
                            newGOCenter.y = 1;
                        }
                        if (newGOCenter.y >= 5)
                        {
                            newGOCenter.y = 5;
                        }
                        if (newGOCenter.x <= 1)
                        {
                            newGOCenter.x = 1;
                        }
                        if (newGOCenter.x >= 9)
                        {
                            newGOCenter.x = 9;
                        }
