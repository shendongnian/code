            try
            {
                double d = double.Parse(str);
                //if works str is a number
            }
            catch (Exception e)
            {
                Console.WriteLine("It isn't a number!");
            }
