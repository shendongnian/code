     protected override void Dispose() 
        {
            try{
            base.Dispose();
             }
           Catch(Exception ex)
             {
                return;
             }
        }
