    public List<string> GetLabelText()
    {
        var labels = new List<string>();
        labels.Add(A1.Text);
        labels.Add(A2.Text);
        // Add all the others
        return labels;
    }
