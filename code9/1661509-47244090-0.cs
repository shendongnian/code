    public enum Operator
    {
        Add,
        Subtract,
        Multiply,
        Divide
    }
    public static IEnumerable<string> Calculate(int num1, int num2, Operator operation)
    {
    }
