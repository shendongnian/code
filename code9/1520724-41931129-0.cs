    public void Parse(string Input)
    {
            int data;  
            if(Input!=null)
            {
                foraech(string subString in Input.split(' '))
                {
                   //call last method
                    last(subString);
                }
            }
     }
