        string[] arr = new string[3] { "Taha", "Usama", "Ali" };
                    Array.Sort(arr);  // C# Provides Direct function to sort array
    
    // Display Result Using Following code
                foreach (string s in arr)
                {
                    Console.WriteLine(s);
                }
