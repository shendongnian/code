    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.I))
        {
            hideCheatGUI = true;
        }
        if (Input.GetKeyDown(KeyCode.Equals))
        {
            // Your code
        }
    }
