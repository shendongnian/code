    public static string starGenerator(int count)
    {
        string stars = string.Empty;
        for(int i = 0; i < count; i++)
            stars += "*";
                
        return stars;
    }
