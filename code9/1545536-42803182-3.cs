        public void SetText(string text)
        {
            textBox1.Text = text;
        }
        public void SetFinished(bool finished)
        {
            checkBox1.Checked = finished;
        }
