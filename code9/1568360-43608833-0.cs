    private bool playing = false;
    private void btnPlay_Click(object sender, System.EventArgs e)
    {
       if (playing)
       {
           // set play image
       }
       else
       {
          // set pause image
       }
       playing = !playing;
    }
