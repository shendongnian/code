        public void Work()
        {
            bool localStop = stop;
            while (!localStop)
            {
                // TODO: actual work without volatile operations
            }
        }
