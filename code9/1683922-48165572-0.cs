    public ActionResult Create(News news, HttpPostedFileBase NewsImageUrl)
    {
         if (ModelState.IsValid)
         {
             if (news.IsSeminar)
             {
             }
         }
    }
