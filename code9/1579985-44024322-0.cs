     public class Packet
     {
          public Dictionary<string, decimal> Compounds { get; }
          public Packet()
          {
               Compounds = new Dictionary<string, decimal>();
          }
     }
