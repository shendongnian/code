    static void Main(string[] args)
        {
            string s = "Test";
             for (int i = 0; i < s.Length; i++)
                {
                    int value = s[i];
                    Console.WriteLine(value);
                }
        }
