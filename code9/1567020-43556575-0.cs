        var tag = e.Node.Tag;
        if (tag is Product)
        {
            var product = e.Node.Tag as Product;
        }
        if (tag is Category)
        {
            var caegory = e.Node.Tag as Category;
        }
