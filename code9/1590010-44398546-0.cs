    private void numericUpDown1_ValueChanged(object sender, EventArgs e)
    {
        int x = Convert.ToInt16(numericUpDown1.Value);
        int x = Convert.ToInt16(numericUpDown2.Value);
        public void tvary(x, y, polomer, gr);
    }
