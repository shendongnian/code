    class Program
        {
            static void Main(string[] args)
            {
                if(args.count() == 0)
                {
                    Sendmail();
                    Downloadfile();
                    ProcessFile();
                    ExportFIle();
                }
                foreach(string s in args)
                {
                    switch(a)
                    case "SendMail":{Sendmail();break;}
                    etc.
                }
            }
        }
