    public static Response EmailContentLoad(this Response)
    {
        if(!response.Successful)
        {
            return response;
        }
        //Do whatever the method normally does
    }
