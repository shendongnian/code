    // Output is : 0 2 4 6 8
    for (int i = 0; i < 10; i++)
    {
       if ((i % 2) == 0) // If i is even,
         //   continue; // continue with next iteration
         Console.Write (i + " ");
    }
