        public void print_days()
        {
            foreach (string day in names_of_days)
            {
                Console.WriteLine(day.Substring(0,3));
            }
            Console.WriteLine();
        }
