    string PackIntoTags(IEnumerable<Entity> entitites)
    {
        string result = string.Empty;
        var enumerator = entities.GetEnumerator();
        while ( enumerator.MoveNext() )
        {
            var current = enumerator.Current;
            result += "<h>";
            result += string.Format("<n>{0}</n>", entity.GeorgeClooney);
            restul += string.Format<"<sq>{0}</sq>", entity.twenieth_revord_of_the_table);
            result += "</h>";
        }
    }
