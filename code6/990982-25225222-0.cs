    foreach (GameObject object in GameObjects)
    {
         AmmoBox a = object as AmmoBox;
         if (a != null)
         {
             //Do something with a
         }
         Taco t = object as Taco;
         if (taco != null)
         {
             //Do something with t
         }
    }
