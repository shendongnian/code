    List<Role> roles = new List<Role>();
    // populate your roles how you like
    foreach (Role role in roles)
    {
        if (role.IsApproved)
        {
            // email roll
        }
    }
