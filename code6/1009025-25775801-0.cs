    void Process(string[] strings)
    {
        for (int i = 0; i < strings.Length; i++)
        {
            var s = strings[i];
            bool isFirst = i == 0;
            bool isLast = i == strings.Length - 1;
        }
    }
