    Console.WriteLine("{0}, {1}, {2}", 
        Math.Max(a, Math.Max(b, c)),  // Get the biggest number
        Math.Max(a, Math.Min(b, c)),  // Get the middle number
        Math.Min(a, Math.Min(b, c))); // Get the smallest number
    
