    if (keyState.IsKeyDown(Key.W))
                        {
                            m_eye = m_eye + new Vector3(0f, 0f, 1f);
                        }
                        if (keyState.IsKeyDown(Key.S))
                        {
                            m_eye = m_eye - new Vector3(0f, 0f, 1f);
                        }
