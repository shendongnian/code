     public UnderGraduate(string classif, string guardian, string addr, string name)
            {
                classification = classif;
                guardianname = guardian;
                address = addr;
                Name= name;
            }
