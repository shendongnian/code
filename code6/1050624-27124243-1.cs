    MyBrick[] brickRecs =
    {
        new MyBrick()
        {
            Rec = new Rectangle(0, 0, 60, 20),
            Image = ...
        },
        new MyBrick()
        {
            Rec = new Rectangle(10, 10, 121, 20),
            Image = ...
        }
    };
     //brickRecs[0].Rec -> get the rectangle for the first brick
     //brickRecs[0].Image -> get the image for the first brick
