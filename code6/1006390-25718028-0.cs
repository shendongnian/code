    List<class> variable= new List<class>();
    for (i = 0; i < count; i++)
           {
               (class)row = (class)sender.Items[i];
               {
                   variable.Add(row);
               }
           }
