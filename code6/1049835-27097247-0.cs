	Console.WriteLine(" A " + " B " + " C ");
	int y = 0;
	int z= 1;
        	Console.WriteLine(" A " + " B " + " C ");
	int y = 0;
	int z= 1;
	for (var x = 1; x <= 10; x++)
	{
		Console.Write(x +" ");
		y+=2;
		Console.Write(y +" ");
	
		
		if (z % 2 != 0)
		{
			Console.Write(z);
			z += 2;
		}
		Console.WriteLine();
	}
