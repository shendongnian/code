    try
    {
        // high level routine that calls deep model code
    }
    catch(Exception exc)
    {
    	MessageBox.Show("An error", 
    		Exception.Message,
    		MessageBoxIcon.Error, 
    		MessageBoxButton.OK);
    }
