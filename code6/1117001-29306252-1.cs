Console.ReadLine()
     string res = Console.ReadLine();
     if (string.Equals(res, "whole numbers", StringComparison.CurrentCultureIgnoreCase);)
     {
           Console.WriteLine("You chose whole numbers.");
     }
