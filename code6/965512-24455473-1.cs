    public void panelView(int pw, int state)
    {
    	switch (pw)
    	{
    		case 1:
    			// stuff
    			break;
    		case 2: 
    			if (state == 1) // Check
    			{
    			    // check setup code
    			}
    			else // Order
    			{
    			    // order setup code
    			}
    			break;
    		case 3:
    		// stuff
    		break; 
    		
    		// etc. etc.
    	}
    }
