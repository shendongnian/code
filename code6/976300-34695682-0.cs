            if (AddColumnFlag)
            {
                for (int i = 0; i < columnNames.Length; i++)
                {
                    GridName.Columns.Add(columnNames[i], columnNames[i]);
                }
            }
