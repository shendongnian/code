    for (int j = 0; j < ny; j++)
    {
        for (int i = 0; i < nx; i++)
        {
            if (!Cells[i, j, 0].IsVirtual)
            {
                // calculate x velocity
                // calculate y velocity
                                
            }
         }
     }
