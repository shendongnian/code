    foreach (Control c in group1.Controls)
            {
                if (c is Label)
                {
                    string name = c.Name;
                    // add your code to add in list or array
                }
            }
