    public DateTime GetDate(DateTime? start = null, DateTime? end = null)
    {
        // Method body...
        if(start == null)
        {
          start = DateTime.MinValue;
        }
    
        //same for end
    }
