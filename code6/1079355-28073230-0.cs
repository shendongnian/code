    class CreateMap
    {
        public void createMap(int size, SpriteBatch spriteBatch, List<Texture2D> texture)
        {
            Tile tile = new Tile();
            // do the work
            return tile;
        }
    }
