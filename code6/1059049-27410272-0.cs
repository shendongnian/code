        do
        {
            line = Console.ReadLine();
            if (line != null)
                numbers[c++] = int.line;
            numbers = new int[c+1];
        } while (!string.IsNullOrEmpty(line));
