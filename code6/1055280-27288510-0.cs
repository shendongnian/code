     public JsonResult FullRetailerUpdate(FormCollection data)
     {
        if (data.AllKeys.Contains("AnElement")) {
          // Your Stuff
        }
        else{
          // Your Stuff
        }   
     }
