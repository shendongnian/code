    private void isMuted = false;
    
    private void onSoundClick(...)
    {
        if (isMuted)
        {
             //Do unmute kind of things
             isMuted = false;
        }
        else
        {
             //Do mute kind of things
             isMuted = true;
        }
    }
