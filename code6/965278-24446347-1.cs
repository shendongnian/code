    try
    {
        // read lines...parse
    }  
    catch(Exception ex)
    {
        try
        {
            // handle line with exception and parse the
        }
        catch(Exception ex)
        {
            // warning error about failing to parse the exceptions
        }
    }
