    for (int r = 0; r < 256; r++)
    {
    	for (int g = 0; g < 256; g++)
    	{
    		for (int b = 0; b < 256; b++)
    		{
                int[] temp = new int[3];
    			temp[0] = r;
    			temp[1] = g;
    			temp[2] = b;
    			colors.Add(temp);
    		}
    	}
    }
