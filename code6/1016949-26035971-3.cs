          if (s[z] == "") continue;                    
          if (s[z].Contains('.'))
          {
                values.Add(s[z])
          } 
          else
          {
                 Name += s[z];
                 str.Add(s[0] + "  " + Name);
          }
       }
