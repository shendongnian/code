    public string UserName;
    
            public info(string name) //get the value from login window into info using constructor
            {
                InitializeComponent();
                UserName = name;
            }
