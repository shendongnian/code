    while (dr3.Read())
                    {
    results= dr3.GetString(12);
                       ResultsFinal= lpDirector.Replace("Open;", string.Empty)
                                         .Replace("Open", string.Empty);
    }
