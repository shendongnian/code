    public void sendImageToAmazon()
    {
        //Send orig image
        //Your current code
        //Send resized images
        //Your current code but use ImageResize(image, 640, 480) - Or something similar!
    }
    public image ImageResize(image orig, int width, int height)
    {
        //Image resizer code.
    }
