    void PerformAction()
    {
        foreach(var item in array1)
        {
            // perfome some actions
            // perfome some actions
            // perfome some actions
            // perfome some actions
            item.DoAction();
        }
        foreach(var item in array2)
        {
            // perfome some actions
            // perfome some actions
            // perfome some actions
            // perfome some actions
            item.DoAction();
        }
        foreach(var item in array3)
        {
            // perfome some actions
            // perfome some actions
            // perfome some actions
            // perfome some actions
            item.DoAction();
        }
    }
