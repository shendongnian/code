    string star = "*"; // use string
    for (int counter = 1; counter <= 10; counter++)
    {        
         Console.WriteLine(star);
         star += "*"; // append a star
    }
    Console.ReadKey();
