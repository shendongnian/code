         static public Label label2 = new Label(); // creates ur label (u can name it label1 if u want)
         public Lobbypage()
        {
            Controls.Add(label2); //im not sure where to place it (see above in the Lobbypage_load function is best i know)
            InitializeComponent();
        }
