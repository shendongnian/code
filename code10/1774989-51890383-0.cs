            string[] a1 = { "A", "B", "C", "D" };
            string[] a2 = { "A", "E", "I", "M", "Q", "U", "Y" };
            for(int i = 0; i < a1.Length; i++)
                for(int y = 0; y < a2.Length; y++)
                    if(a1[i]==a2[y])
                    {
                        //Code
                    }
            
