    public string Name 
       {
          get => name; // expression that resolves to getting this.name
          set => name = value; // expression that resolves to setting the passed value to this.name
       }
