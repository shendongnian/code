    SingleRecord =>
        {
            int IntVal = 0;
            string StringVal = string.Empty;
            IntVal = some logic;
            StringVal = SingleRecord + some logic; 
            MyTable.Rows.Add(IntVal, StringVal);
        }
