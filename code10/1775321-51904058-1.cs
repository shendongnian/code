        //some code
        int ID = int.Parse(Console.ReadLine());
        If(!ID_Prod.Contains(ID))
        {
            ID_Prod.Add(ID);
            //append other lists
        }
            //update lists
        }
     }
