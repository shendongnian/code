    Dictionary<string, object> objects = new Dictionary<string, object>();
    
                for (int i = 0; i < 5; i++)
                {
                    objects.Add("obj" + i, new object());
                }
