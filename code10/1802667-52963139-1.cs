    public class Custom
    {
        // ... your class
        public bool Equals(Custom other)
        {
            return (attr1 == other.attr1) && (attr2 == other.attr2) && ... ;
        }
    }
