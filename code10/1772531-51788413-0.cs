    public static double? GetAngle(double deltaX, double deltaY)
    {
        if (deltaX == 0 && deltaY == 0)
        {
            return null;
        }
        // ...
        // Calculations etc
        return result;
    }
