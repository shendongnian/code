    I changed to test if the reader had a record or not.
         if (reader6a.Read())
            {
                div1.Visible = true;
                div2.Visible = false;
                div3.Visible = false;
            }
        else
    {
    and so on
    }
