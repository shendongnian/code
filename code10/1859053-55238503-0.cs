    public void Set(hostname = null, username = null, password = null)
    {
        if (hostname != null)
            Hostname = hostname;
        if (username != null)
            Username = username ;
        ...
    }
