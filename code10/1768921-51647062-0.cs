    foreach(ShelfInventoryModel item in output)
                {
                    Console.WriteLine($"{item.Quantity} {item.ItemName}");
                    
                }
                Console.ReadLine();
            }
