    List<string> myCollection = new List<string>();
            
      if (comboBoxName.SelectedIndex >= 0)
      {
        myCollection.Add(comboBox1.SelectedItem.ToString());
      }
      if (comboBoxStrasse.SelectedIndex >= 0)
      {
        myCollection.Add(comboBox2.SelectedItem.ToString());
      }
