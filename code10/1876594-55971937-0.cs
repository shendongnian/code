    List<A> listA = New List<A>;
        if (listA != null)
        {
              foreach (var temp in listA)
              {
                 foreach(var d in temp.C) 
                 {
                     //ToDo Interact with d.D
                 }
              }
        }
