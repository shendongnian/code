        struct Person
        {
            //...
    
            public override string ToString()
            {
                return firstName + " " + lastName + ", age " + age;
            }
        }
