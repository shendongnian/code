    public static void RandomFill(int[,] array, int rows, int columns)
    {
        array = new int[rows, columns]; // <--- Remove this line since its array
                                        // is already initialised prior of calling
                                        // this method.
        .........
    }
