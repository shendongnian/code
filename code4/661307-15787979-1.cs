    public void methodName(param,param)
    { 
        foreach loop(param)
        {
         If (item != null)
         {
          DateTime newDate = item.Date.value;
          {
            AClassOfMine a = new AClassOfMine();
            item.item.date = a.CalculateNewDate(newDate);
          }
        }
    }
