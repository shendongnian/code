    // if red then change to blue
    if (label1.BackColor == Color.Red)
    {
        label1.BackColor = Color.Blue;
    }
    // if blue then change to red
    if (label1.BackColor == Color.Blue)
    {
        label1.BackColor = Color.Red;
    }
