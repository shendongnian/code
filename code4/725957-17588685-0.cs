        int[,] a = new int[,]
    	{
    	    {2, 4}
    	};
    
    	for (int i = 0; i <= a.GetUpperBound(0); i++)
    	{
    	    for (int k = 0; k <= a.GetUpperBound(1); k++)
    	    {
    	        // a[i, k]; 
    	    }
    	}
