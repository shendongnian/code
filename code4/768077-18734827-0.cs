    private void tabControl1_SelectedIndexChanged(Object sender, EventArgs e) {
       switch((sender as TabControl).SelectedIndex) {
            case 0:
                // Do the autofill if this is the config tab else on the the index it is.
                break;
            case 1:
                break;
        }
    }
