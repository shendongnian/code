      for (int i = 0; i < bgList.Count; i++)
            {
                var bglistitem = new Sprite()
                bhlistitem.Scale = 2.0f;
                bgList.Add(bglistitem);
            }
