       var t=new Thread(() => { });
                if (t.IsAlive)
                {
                    //thread is still alive
                }
                else
                {
                    //not alive
                }
