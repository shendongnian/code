     List<int> index = new List<int>();
     for (int i = 0; i < txtLines.Count(); i++ )
             {
              string text = tetLines.AtElement(i);
               index.Add(text.Count);
             }
