    List<object> myProperties = new List()<object>
    {
        prop1, 
        prop2, 
        prop3,
        prop4, 
        prop5, 
        prop6 
    };
    foreach(var p in myProperties)
    {
        myList.Add(p);
    }
