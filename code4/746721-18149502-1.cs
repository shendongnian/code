    public static class HelperClass
    {
        public static string FrenchSave(B frenchInput)
        {
            
            string result = string.Format("ID = {0}; Name = {1}; City = {2}", xx.Id, xx.Nom, xx.ville);
            return result;
        }
        public static string EnglishSave(A englishInput)
        {
            string result = string.Format("ID = {0}; Name = {1}; City = {2}", xx.Id, xx.name, xx.city);
            return result;
        }
    }
