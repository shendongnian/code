    Dictionary<int, KeyValuePair<string, string>>  GetFileProps(string path)
    {
        System.Drawing.Image image = System.Drawing.Image.FromFile(path);
    
        //Implement the rest based on props of image
    }
