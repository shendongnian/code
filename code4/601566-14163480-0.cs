    for (var row = 0; row < c_tbl.Rows.Count; row ++)
    {
        for (var col = 0; row < c_tbl.Columns.Count; col ++)
        {
            switch (c_tbl[col, row].Value)
            {
            }
        }
    }
