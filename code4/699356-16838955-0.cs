     var items = Enumerable.Range(0, 100000);
     foreach (int item in items)
     {
         if (item % 3 == 0)
         { 
             //...
         }
         else
         {
             //...
         }
     }
