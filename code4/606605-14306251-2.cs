    foreach(var group in query)
    {
        // group.Category
        foreach(var item in group.Items)
        {
            // item.ItemName
            // item.QtyNeeded
            // item.QtyFulfilled 
        }
    }
