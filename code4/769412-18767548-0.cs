    foreach(string item in myList)//Iterate throught each item
    {
     if(item.Contains("Search Term")//True if the item contains search pattern.
     {
       return item;//Return the matched item.
     }
    }
