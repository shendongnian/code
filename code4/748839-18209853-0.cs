         for (int i = 0; i < xmlNodeList.Count; i++)
            {
                XmlNode xmlNode;
            
                xmlNode = xmlNodeList.Item(i).SelectSingleNode("ProductName");
                //xmlNode.InnerText;
            }
