    if( int.TryParse(textBox2.Text.Trim(), limit) ){
        //This code will run only if textBox2.Text is an int
        for (var i = 0; i < limit ; i++)
        {
          //code in here
        }
    }
