        List<string> newList = new List<string>();
        for (int i = 0; i < 5; i++)
        {
            if (test[i].Contains("world"))
            {
                newList.Add(test[i]);
            }
        }
