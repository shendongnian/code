            try
            {
               if (!counties.ContainsKey(items[0)
               {
                 countries.Add(items[0], items[1]);
               }
            }
            catch (Exception ex)
            {
                  //MessageBox.Show(ex.Message);
    
            }
