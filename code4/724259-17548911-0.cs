    	for (int i = 0; i < 4; i++)
        {
             if ((i+1) < list.Count)
                { 
                  // populate the textbox
                 }
             else
                 {
                   // disable the textbox
                 }                 
        }
