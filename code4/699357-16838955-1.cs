     var items = Enumerable.Range(0, 1000000);
     for(int i=0;i<items.Length;i++)
     {
         if (item % 3 == 0)
         { 
             //...
         }
         else
         {
             //...
         }
     }
