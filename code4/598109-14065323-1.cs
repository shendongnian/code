    private static void MultiCastDelMethod(int x, int y)
    {
        try
        {
            int zero = 0;
            int z = (x / y) / zero;
        }
        catch (Exception ex)
        {
            throw ;
        }
    }
