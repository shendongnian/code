    for(int i = MAX_SERIALIZATION_ITEMS; i < EpsItems.Count; i++)
    {
        EpsItems.RemoveAt(MAX_SERIALIZATION_ITEMS);
    }
    
    for(int i = MAX_SERIALIZATION_ITEMS; i < OthItems.Count; i++)
    {
        OthItems.RemoveAt(MAX_SERIALIZATION_ITEMS);
    }
