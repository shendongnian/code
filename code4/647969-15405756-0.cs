    List<string> newList = new List<string>();
    for (int i = 0; i < list1.Count && i < list2.Count && i < list3.Count; i ++)
    {
        newList.Add(list1[i]);
        newList.Add(list2[i]);
        newList.Add(list3[i]);
    }
