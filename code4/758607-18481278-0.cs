    for (int i = 0; i < city.GetLength(0); i++)
    {
        for (int j = 0; j < city.GetLength(1); j++)
        {
            Console.Write(city[i, j] + "\t");
        }
        Console.WriteLine();
    }
