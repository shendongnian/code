    void processDataAllData(IEnumerable<Item> data,  setting blah)
    {
        switch(blah)
        {
          case blah.single:
            foreach (Item item in data)
            {
            
            }
        }
        // next case, next loop ...
    }
