    public static double CalculateStandardDeviation<TSource>(IEnumerable<TSource> values)
    { 
        // If Has a possible convertion    
        // Convert
        // foreach (var v in values) 
        //     TypeDescriptor.GetConverter(typeof(T)).ConvertFrom(v);
        // Your logic using doubles
    
        // return ...;
    }
