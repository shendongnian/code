      TableRow row = new TableRow();
      TableCell cell = new TableCell();
      cell.ForeColor = System.Drawing.Color.Red;
      cell.Text = "some text";
      row.Cells.Add(cell);
      Table1.Rows.Add(row);
