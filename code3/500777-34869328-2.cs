        public ActionResult Index([NullModel] SearchData query)
           {
              // I'd like to be able to do this
              if (query == null)
              {
                  // do something
              }
           }
