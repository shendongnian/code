    public class CustomDialog
    {
        List<Customer> _customerList;
        public CustomDialog(List<Customer> customers)
        {
            _customerList = customers;
        }
        // Fill the combo with the _customerList saved in the constructor.
    }
