    Saving s = array[i] as Saving;
    if(s != null)
    {
       s.SomeSavingMethod();
    }
    else
    {
       Checking c = array[i] as Checking;
       if(c != null)
          c.SomeCheckingMethod();
    }
