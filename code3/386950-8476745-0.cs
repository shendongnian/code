    public void Crawl()
    {
       //dequeue
       if(token == "TERMINATION")
          return;
       //get child links
       if(no more child links)
          queue.Enqueue("TERMINATION");
       else
          //enqueue child links
    }
