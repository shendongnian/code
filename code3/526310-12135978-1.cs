     DataRow row;
        for(int currentRow=0; currentRow < table.Rows.Count; currentRow++)
        {
             row = table.Rows[currentRow];
             foreach (DataColumn c in table.Columns)
                {
                    if(c.ColumnName == "Product")
                    {
                         //Column is product
                    }
                }
                      
        }
