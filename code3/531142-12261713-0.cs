    try
     {
        // your code to run
     }
     catch(SqlExecption ex)
      {
         Console.WriteLine(ex.Message);
      } 
      // Catch all Execption
     catch(Exepction)
      {
         Console.WriteLine(Message);
      }
