    if(DateTime.TryParse(rdr["postingdate"].ToString(), rdrDateTime))
    {
        // proceed as planned
        if (rdrDate.Month == DateTime.Now.Month) 
        {
           // and so on.
        }
    }
    
