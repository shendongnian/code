    Task.Factory.StartNew(() =>
       {
            try
            {
            // setup
            // var stuff = ParseFile()
            // foreach(var row in stuff)
            //   do more stuff
            // create summary text file
            // Finalize
                return someFileName;
            }
            catch(Exception e)
            {
                return string.Empty;
            }
       });
