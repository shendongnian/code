    for (int r = 0; r <= 255; r++)
                {
                    for (int g = 0; g <= 255; g++)
                    {
                        for (int b = 0; b <= 255; b++)
                        {
                            button1.BackColor = Color.FromArgb(r, g, b);
                        }
                    }
                }
