     MovesMade m = new MovesMade();
     m.Peep = new Player();
     m.Peep.Name = "foo";
     AlllMoves.Add(m);
     foreach(var moves in AlllMoves)
     {
        if (moves.Peep.Name = "Jules")
        {
        }
     }
