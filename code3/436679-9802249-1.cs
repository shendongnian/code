    public class ResponseHandler<T>
    {
        public ResponseHandler(Action<T> check)
        {
            this.CheckVariables = chek;
        }
        Action<T> CheckVariables;
        private void callResponseCallback<T>...
        {
            // stuff
            CheckVariables(response);
            // stuff
        }
    } 
