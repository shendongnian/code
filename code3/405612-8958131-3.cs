    int x = content.GetUpperBounds(0)
    int z = 0;
    int i - 0;
    List<string> list = new List<int>();
    for (i = 0; i <= x / 3; i++)
        {
            z = (i * 3);
            list.Add(content[z]);
        }
    id = list.ToArray();
