    try
    {
       //SomeCode;
    }
    catch (Exception ex)
    {
        MessageBox.Show(ex.Message, 
                        "Error Information", 
                        MessageBoxButtons.AbortRetryIgnore,
                        MessageBoxIcon.Exclamation);
    }
