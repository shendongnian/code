    private void numericUpDown1_MouseUp(object sender, MouseEventArgs e)
    {
        if (e.Button==MouseButtons.Left)
        //Expensive 3d routines
        MessageBox.Show(numericUpDown1.Value.ToString());
    }
