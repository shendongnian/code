    // simplified example
    
    public static string AddSuper( string value ){
       if( [ regex here to test for pattern, like "\d{1}st" ] ){
          return [ regex replace value with "<sup>value</sup>" ];
       }
    }
