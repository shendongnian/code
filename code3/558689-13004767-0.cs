    private void GetSpacedOptions(bool zeroPad = false)
    {
        string str = "";
        if(zeropad)
        {
            str = //
        }
        else
        {
            str = //
        }
        //Then use str in code
        var options = //
    }
