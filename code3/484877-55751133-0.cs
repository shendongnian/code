            private void SelectionChangedProtocoleApi(object sender, SelectionChangedEventArgs e)
        {
            ComboBox combo = sender as ComboBox;
            if (combo.SelectionBoxItem.GetType() == typeof(ModelProtocoleApiConfiguration))
            {
                var tmp = 1;
            }
        }
