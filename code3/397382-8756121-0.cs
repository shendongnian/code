        protected void Page_Load(object sender, EventArgs e)
        {
            if(this.IsPostBack)
            {
                merchantId.Text // to get text
                PaymentInformation newPayInfo = new PaymentInformation();
                /* Validation of input goes here ... */
                // Then send data.
            }
        }
