	Dictionary<int, List<string>> values = new Dictionary<int, List<string>>();
	foreach (KeyValuePair<int, List<string>> node in values)
	{
		for (int i = 0; i < node.Value.Count; i++)
		{
			//Whatever
		}
	}
