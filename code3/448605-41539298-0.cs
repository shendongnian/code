                    // JIT optimization.
                    if (i == 0)
                    {
                        // Discard every result you've collected.
                        // And restart the timer.
                        stopwatch.Restart();
                    }
