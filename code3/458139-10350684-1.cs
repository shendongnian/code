        routes.MapRoute(
                "filters",
                "All",
                new { controller = "Contracts", action = "All" }
            ); 
           routes.MapRoute(
                "filters1",
                "focus",
                new { controller = "Contracts", action = "focus" }
            ); 
