    int person=1;
    int i;
    int[] Fruits = new int[numFruits];
    int length = Fruits.Length;
    ArrayList Amelia = new ArrayList();
    ArrayList Jessica = new ArrayList();
    ArrayList Bruno = new ArrayList();
    for(i = 0; i < length; i++)
        {
        if(person==1)
            {
            Amelia.Add(Fruits.Min);
            }
        if(person==2)
            {
            Jessica.Add(Fruits.Min);
            }
        if(person==3)
            {
            Bruno.Add(Fruits.Max);
            person==1
            }
        }
