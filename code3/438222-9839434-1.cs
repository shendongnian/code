    if (CheckBoxBranch.Checked == true)
    {
      // code
      CheckBoxBuyer.Checked = false;
    }
    else if (CheckBoxBranch.Checked == false)
    {
      panelBranch.Visible = false;
      // code
    }
