	     foreach(var innerArray in multiDynamic)
         {
                 foreach(var item in innerArray)
                 {
                   Console.WriteLine(item);
                 }
                 Console.WriteLine();
         }        
