    string mainMenuSelection = String.Empty;
    
    bool parsed = byte.TryParse(Console.ReadLine(), out mainMenuSelection);
    switch(mainMenuSelection)
    {
     case...
     break;
     default:
        .... TODO
    }
