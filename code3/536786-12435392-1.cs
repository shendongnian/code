    private void viewscreen_MouseDown(object sender, MouseEventArgs e)
        {
            if (e.Button == MouseButtons.Right)
            {
                generate_editpanel();
            }
        }
