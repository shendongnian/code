     public partial class MainWindow : Window
        {
            public MainWindow()
            {
                MessageBox.Show(string.Empty);
                InitializeComponent();
            }
        }
