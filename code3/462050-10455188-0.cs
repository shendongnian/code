    foreach (Control c in parentForm.Controls)
    {
       if (c is Label)
       {
           // Do label stuff here
       }
       else if (c is TextBox)
       {
           // Do text box stuff here
       }
       if (c is Panel)
       {
           // Do panel stuff here
       }
    }
