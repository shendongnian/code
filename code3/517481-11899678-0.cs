    int i = 0;
    while(i == 0)
    {
        i == 1; // this will make it exit the loop unless case 4 happens   
        switch(ch)
        {
            case 0: {
                Console.WriteLine("Addition value is :{0}", a + b);
                break;
            }
            ...
            case 4 {
                i == 0;
                break;
            }
         }
    }
