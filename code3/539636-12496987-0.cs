             if ( typeof (Int32) == dgv1.SelectedCells[0].Value.GetType())
            {
                // your business logic 
            }
            else if (typeof(string) == dgv1.SelectedCells[0].Value.GetType())
            {
                // your business logic 
            }
