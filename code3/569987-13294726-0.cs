    void item_PropertyChanged(object sender, PropertyChangedEventArgs e)
    {
        switch(e.PropertyName)
        {
            case "Name":
                //You Code
                break;
        }
        // Alternatively, you can use Reflection to set the property 
        // instead of building a switch case.
    }
