     foreach (string key in oldtable.Keys)
        {
            if (newtable.Contains(key))
            {
                // do your work;
            }
            else
            {
                // do your work;         
           }
        }
