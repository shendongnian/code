    foreach (object o in result.ToList())
            {
                if (o.Equals(target))
                {
                    myResult = o;
                    break;
                }
            }
 
