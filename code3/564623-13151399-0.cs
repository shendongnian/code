    Envelope env;
    public override Envelope Parse(string input,out env)
    {
    
        //intialize env here
    }
    
    string s;
     public override string Format(Envelope env,out s)
    {
    
       //initialize string here
    }
