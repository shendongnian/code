    Dictionary<string,int> namesAndAges = new Dictionary<string,int>();
    string line = "";
    while(there's still xml to read)
        {
            line = blah; //read a line of xml however you were gonna do it
            namesAndAges.Add(line.Split(',')[0] , line.Split(',')[1]);
        }
    
