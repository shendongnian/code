    for (int i = 0; i < xList.Count; i++) {
      Console.WriteLine(xList[i] + " " + yList[i]);
    }
    for (int i = xList.Count - 1; i >= 0; i--) {
      Console.WriteLine(xList[i] + " " + (-yList[i]));
    }
