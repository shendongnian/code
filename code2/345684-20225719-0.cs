                DateTime? myDate;
            if (TextBoxWithDate.Text != "")
            {
                myDate = DateTime.Parse(TextBoxWithDate.Text);
            }
            else
            {
                myDate = null;
            }
  
