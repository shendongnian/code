    for (.......)
    {
        DateTime dt = DateTime.Now;
        // Here goes your code...
        TimeSpan ts = DateTime.Now - dt;
        // Check ts and do what you need
        // Example: textbox1.Text = ts.TotalMilliseconds.ToString();
    }
