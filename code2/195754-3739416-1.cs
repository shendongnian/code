    private long myPrime;
    public long Prime {
       get { return myPrime; }
       set { 
         if (prime(value) {
            myPrime = prime;
         }
         else {
            //throw an error or do nothing
         }
       }
    }
