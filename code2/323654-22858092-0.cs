     use name space then using System.Linq; 
     use normal string to check whether the string         
     consist   at lest  one character or number.
    using System.Linq; 
 
    check the string has characters --->  String.Any(char.IsLetter) 
    check the string has numbers   --->   String.Any(char.IsDigit)
    if (String.Any(char.IsLetter) && String.Any(char.IsDigit))
    {
    //statement goes here.....
    }
    sorry for the late reply ...
