    string msg = "";
    for (int i = 0; i < max; i++)
    {
        msg += "Your total is ";
        msg += "$500 ";
        msg += DateTime.Now;
    }
    StringBuilder msg = new StringBuilder();
    for (int j = 0; j < max; j++)
    {
        msg.Append("Your total is ");
        msg.Append("$500 ");
        msg.Append(DateTime.Now);
    }
