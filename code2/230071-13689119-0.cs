    List<string> list = new List<string>();
    // add items
    
    foreach (string text in list)
    {
        if (text.Equals(list[list.Count - 1]))
        {
            // last item
        }
        else
        {
            // not last item
        }
    }
