    if (file != null)
    {
        string extension = Path.GetExtension(file.FileName);
        if (extension != null)
        {
            switch(extension.ToLower())
            { 
              //..snip
            }
        }
    }
