    if (ControlMode == SPControlMode.Edit || ControlMode == SPControlMode.New)
    {
        // Render for Editing or New mode control
    }
    else if (ControlMode == SPControlMode.Display)
    {
        // Render for Display mode
    }
