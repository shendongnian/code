            List<string> lst = new List<string>();
            for (int i = 0; i < 5; i++)
            {
                lst.Add("Sa" + i.ToString());
            }
            cmb.ItemsSource = lst;
