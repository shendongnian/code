    if (employee != null) {
        string name = employee.Name.TrimStart();
        if (!string.IsNullOrEmpty(name)) {
            char firstChar = name[0];
            // Use it....
        }
    }
