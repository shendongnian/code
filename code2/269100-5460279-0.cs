     public string Parse(string fileName)
     {
         string res = null;
         try
         {
            /// parse the file, assign parse result to res
         }  
         catch
         {
            res = null;
         }
         return res;
     }
