        for (int i = 0; i < nameList.Count; i++)
            {
              if (nameList[i].Equals(name,StringComparison.OrdinalIgnoreCase))
                {
                    TempList.Add(nameList[i]);
                }
            }
