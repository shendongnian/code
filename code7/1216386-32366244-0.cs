     string name;
     bool even = false;
     foreach(var text in items)
     {
         if (even)
         {
             var password = item;
             // check name, password here 
         }
         else
         { 
             name = text;
         }
         even = !even;
      }
