    else
      {
         if(int.TryParse(prueba.Substring(prueba.LastIndexOf('_')),out num))
           {
             //proceed your code
           }
         else
           {
             //Throw error message
           }
       }
