    for (int i = 0; i < values.Count(); i++)
         CsvList.Add(
              new Csv()
              {
                   Position = i.ToString(),
                   Name = values[i]
              });
