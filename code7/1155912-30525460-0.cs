    private Button makeBtn(string caption)
    {
       var button = new Button();
       button.Enabled = true;
       button.Visible = true;
       button.Caption = caption;
       return button;
    }
