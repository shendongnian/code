    foreach(var item in coordGeomItems)
    {
        var curve = item as Curve;
        if (curve != null)
        {
            // access curve properties here
        }
        var spiral = item as Spiral
        if (spiral != null)
        {
            // access spiral properties here
        }
        // ...
    }
