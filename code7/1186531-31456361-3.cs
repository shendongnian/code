    bool flag = false;
        if (!flag)
        {
            if (variable > 1)
            {
    
                Console.Writeline(DateTime.UtcNow.Ticks);
                flag = true;
            }
        }
