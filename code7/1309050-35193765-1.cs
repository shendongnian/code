        Service1 _proxy = new Service1();
        var result = _proxy.GetList();
        
    List<ItemDataContract> df = new List<ItemDataContract>();
     foreach (var data in result )
                {
                    df.Add(df);
                }
    return df;
