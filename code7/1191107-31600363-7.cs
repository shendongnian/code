    // Open connection
    for(int row = 0; row < rowCount; row++) {
        // Build query
        for(int i = 1; i <= 20; i++) {
            // Set query parameters
        }
        //Execute query
    }
    // Close connection
