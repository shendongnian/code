     strResponse = PostJson(Settings.LoginServer, Settings.LoginResourceUrl, request);   
     if (string.IsNullOrEmpty(strResponse))
     {
         //Error
     }
     else
     {
          //Success
     }
