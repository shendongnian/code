    public class ObjectSelectionViewModel : ObjectViewModel
    {
        // The current list of selected items.
        public List<ObjectViewModel> SelectedItems { get; }
        public override string Name
        {
            get 
            {
                if (SelectedItems.Count > 1) {
            }
           
        }
    }
