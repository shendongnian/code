    if (pwd.Text == "enter password" || pwd.Text == "")
            {
                //Do nothing. Enter Password text will be seen
            }
            else
            {
                pwd.PasswordChar = '*';
            }
