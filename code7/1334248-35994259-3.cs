        public void Save(string savePointName) {
            //SNIP
            try {
                    //SNIP
                    _internalTransaction.Save(savePointName);
                }
            //SNIP
        }
