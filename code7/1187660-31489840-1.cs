      <DataGrid
           DataContext="{StaticResource SomeViewModel}"
           IsReadOnly="{Binding IsEditable}">
      </DataGrid>  
      <DataGrid
           DataContext="{StaticResource SomeViewModel}"
           IsReadOnly="{Binding IsEditable}">
       </DataGrid>  
