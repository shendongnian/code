    while (true)
    {
        if (!Keyboard.IsKeyDown(Key.V))
        {
            while (Keyboard.IsKeyDown(Key.LeftCtrl) || Keyboard.IsKeyDown(Key.RightCtrl))
            {
                if (Keyboard.IsKeyDown(Key.V))
                {
                            
                }
            }
        }
    }
