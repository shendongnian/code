    //Method
    private void changeBackgroundOfButtons(string selectedButtons)
        {
            switch (selectedButtons)
            {
                case "home":
                    HomeButton.Background = (SolidColorBrush)new BrushConverter().ConvertFromString("#0199ED");
                    TextBlock1.Foreground = Brushes.White;
                    TextBlock2.Foreground = (SolidColorBrush)new BrushConverter().ConvertFromString("#6a6a6a");
               case:
                 //For all my buttons
            }
        }
