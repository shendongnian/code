    class TimeLogLineParser
    {
    	public TimeLogData ParseLine(string line)
    	{
    		TimeLogData result = new TimeLogData();
    
    		// here write logic to parse line of code into TimeLogData
    
    		return result;
    	}
    }
