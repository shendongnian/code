	int sin;
    Console.Write("Please enter sin number: ");
    sin = int.Parse(Console.ReadLine());
    string digits;
    do 
	{
		Console.Write("Please enter sin number: ");
    	digits = Console.ReadLine();
        if (digits.Length > 9 || digits.Length < 9)
        {
            Console.WriteLine("Error invalid entry");
        }
	}
	while (while (digits.Length > 9 || digits.Length < 9);
