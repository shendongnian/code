    var items = new[] { "CustomerName", "CustomerID", "ContactNumber" };
    foreach (string str in items)
        Console.WriteLine(str.SeparateTitleCase());
    Customer Name
    Customer ID
    Contact Number
