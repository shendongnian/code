     private async Task<bool> RefreshContacts()
        {
            Task refresh = Task.Run(() =>
            {
                try
                {
                    // run the query
                          
            }
            catch { }
    
     if(WasSuccesful)
                    {
                        return true;
                    }
                   else
                   {
                        return false;
                   }  
        }
        );
          
    }
