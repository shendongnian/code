            richTextBox.BeginChange();
            try
            {
                // Add column
                // For each row, add a cell to the column.
            }
            finally
            {
                richTextBox.EndChange();
            }
