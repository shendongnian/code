    // use Int64 to allow larger numbers. Otherwise stick to the classic int type
    private Int64 intNum;
    
    public Int64 IntNum
    {
        get { return intNum; }
        set { intNum = value; }
    }
