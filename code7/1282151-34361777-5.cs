    public App()
        {
        /*
            stuff
         */
        Suspending += (s, a) =>
            {
                SaveTheData(); // I really like my data and want it for later too
            };
        }
