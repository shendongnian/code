    string fileSpec = 
        Path.Combine(Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location)),
                     "Images",
                     subfolderName,
                     imageName + ".png");
