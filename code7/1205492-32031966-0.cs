    while(true)
    {
        Console.WriteLine("What do you want to move next?");
        var move = Console.ReadLine().ToLower();
        while (true)
        {
            switch (move)
            {
                // Move player...
            }
        }
    }
