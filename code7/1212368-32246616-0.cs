    if((string.IsNullOrEmpty(name) || Member.FirstName == name) && 
    (string.IsNullOrEmpty(email) || Member.Email == email)) //etc
    {
      // do stuff
    } else {
      // do other stuff
    }
      
