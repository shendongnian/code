    public static class KeyF
    {
        public void RunF(KeyEventArgs e)
        {
            switch(e.Key)
            {
               // Add cases for F keys.
              // And do stuff.
            }
        }
    }
