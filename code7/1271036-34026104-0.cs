    [HttpPost]
     public ActionResult SendMail(SendMail SendMail)
     {
      if (...)
      {
        //Sending Mail SMPT Setting    
        return RedirectToRoute(URL);
      }
      else
      return RedirectToRoute(URL);
    }
