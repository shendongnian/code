    public void AddAccount(string name, string addres, string username, string password)
    {       ....
          if (OpenConnection())
          {
             ///continue
          }
          else
          {
            ///log error
          }
    }
