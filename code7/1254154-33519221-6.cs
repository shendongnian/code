    class MyCalc
    {
        private static readonly double spi = Math.Sin(Math.PI);
        private static readonly double pi = Math.PI;
        public void Execute()
        {
            // .. whatever
        }
    }
