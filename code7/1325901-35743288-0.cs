    foreach(Control c in panel1.Controls)
    {
         CheckBox cb = c as CheckBox;
         if (cb!=null)
         {
              if (cb.Checked)
              {
                   //Add code
              }
         }
    }
