    // Evaluate the right hand side:
    int incrementResult = c;   // Store the original value
    c = c + 1;                 // Increment c
    // Perform the assignment:
    c = incrementResult;       // Assign c to the result of the increment
