    DataTable dt = GetData(type.Text, 
                           category.Text,
                           subsidary.Text,
                           country.Text, 
                           date.Text.TryGetDateTime());
