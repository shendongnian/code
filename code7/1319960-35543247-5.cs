    private static void utskrift(Box box, int choice)
    {
        if (choice== 1)
        {
            ...
            Note addnote = new Note(note, priority);
            box.AddNote(addnote);
            ...
        ...
    }
   
