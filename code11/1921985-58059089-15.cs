     public void PreparePrintContent(GenericManifestPDF page)
        {
            firstPage = null;
            if (firstPage == null)
            {
                firstPage = page;
            }
        }
