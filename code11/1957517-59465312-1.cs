      static void Main(string[] args) {
        //TODO: validate args
        // When given args we parse them...  
        int a = int.Parse(args[0]);
        int b = int.Parse(args[1]);
        // and send the result into standard output
        Console.WriteLine(a + b);
      }
