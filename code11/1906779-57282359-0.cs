       private static int ConvertToNumber(string input)
        {
            switch (input)
            {
                case "Five":
                    return 5;
                    // do the remaining work
                default:
                    return 0;
            }
        }
