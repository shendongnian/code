    private void WindowMouse(object mouseButton)
    {
        if (mouseButton is MouseButton m)
        {
            if (m == MouseButton.Left)
            {
                // DragMove();
            }
        }
    }
