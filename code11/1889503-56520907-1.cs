    line = sr.ReadLine();
    while(line != null)
    {
        if (line[0] =='B') || line[0] == 'b'))
        {
            performListBox.Items.Add(line);
        }
            //performListBox.Items.Add(line);
        line = sr.ReadLine();
    }
