     ArrayList arrayList = new ArrayList();
     for (int i=0;i<=6;i++) {
         arrayList.Add(i);
     }
     for (int i = 0; i <= 6; i++)
      {
         System.Diagnostics.Debug.WriteLine("id = " + arrayList.Get(i));
      }
