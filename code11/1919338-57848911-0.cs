    public class FullAdder
    {
       AndGate gate1 = new AndGate();
       OrGate gate2 = new OrGate();
       // ... other required gates
    
       public bool GetSum(bool value1, bool value2, bool carry)
       {
          // code for sum using the above gates
       }
    
       
       public bool GetCarry(bool value1, bool value2, bool carry)
       {
          // code for carry using the above gates
       }
    }
