int pallet = 1;
int[] res = { 5, 1 };
foreach (int element in res)
{
  var resu = res.Length;
  if (resu < pallet)
    { }
  else { }
}
int i = 0;
foreach (int element in res)
{
  if (i <= pallet)
    { }
  else { }
  i++;
}
