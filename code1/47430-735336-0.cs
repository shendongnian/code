    template<class T>
    T ImageMatrix::GetRotatedCopy(VDouble angle)
    {
      // ... create a new instance of ImageMatrix and return it.
    }
    // Add the following line
    template ImageFilter ImageMatrix::GetRotatedCopy<ImageMatrix>(VDouble);
