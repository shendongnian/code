    string hexValue = "000000"; // You don't need the hash
    Color colour = System.Drawing.ColorTranslator.FromHtml(hexValue);
    if (colour == System.Drawing.Color.Black)
    {
        // Yippee
    }
