    MessageBoxResult m = MessageBox.Show("The file will be saved here.", "File Save", MessageBoxButton.OKCancel);
    if(m == m.Yes)
    {
        // Do something
    }
    else if (m == m.No)
    {
        // Do something else
    }
