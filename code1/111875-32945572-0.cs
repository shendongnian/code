    string speech = e.Result.Text;
    switch (speech)
    {
        case "next":
        {
            btnNext.PerformClick(); //use your own button's name instead of btnNext
            break;
        }
    }
