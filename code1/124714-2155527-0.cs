    public Form1()
    {
        InitializeComponent();
        comboBox1.DisplayMember = "Name"; // use the Name property from items 
                                          // in the list when displaying them
    }
