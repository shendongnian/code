        using(Bitmap bmp = (Bitmap)System.Drawing.Image.FromFile(sessID))
        {
             //.. your code...
             using(MemoryStream.... etc)
             {
                 ....
             }
        }
