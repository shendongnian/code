    // mystrings is a string array
    List<string> myList = new List<string>();
    foreach(string s in mystrings)
    {
        if(s.Length > 5)
        {
            myList.add(s);
        }
    }
