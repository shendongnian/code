    Dictionary<string, string> d = new Dictionary<string, string>();
    string s1 = "user=u123;name=Test;lastname=User";
    foreach (string s2 in s1.Split(';')) 
    {
    	string[] split = s2.Split('=');
    	d.Add(Strings.Split(0), Strings.Split(1));
    }
