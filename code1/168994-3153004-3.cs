         if (!System.IO.File.Exists(sanitized))
         {
             ...
         }    
         else 
         {
             foreach (string names in existingNames)
                 {
                     ...    
                 }
             }
         }
