    if (Environment.OSVersion.Versino.Major == 5)
    {
        if (Environment.OSVersion.Minor == 1)
        {
                 // XP
        }
        else if (Environment.OSVersion.Minor == 2)
        {
                 // Server 2003.  XP 64-bit will also fall in here.
        }
    }
    else if (Environment.OSVersion.Version.Major == 6)
    {
            // Vista on up
    }
