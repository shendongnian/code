            ArrayList list=new ArrayList();
            list.Add(1);
            list.Add(2);
            list.Add(null);
            list.Add(4);
            foreach (int i in list)
            {
                System.Diagnostics.Debug.WriteLine(i);
            }
