       public IList<int> myiterator()
            {
                List<int> lst = new List<int>();
                for (int i = 0; i <= 4; i++)
                {
                    lst.Add(a[i]);
                }
    
                  return lst;
            }
