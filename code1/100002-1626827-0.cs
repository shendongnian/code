    // Returns null if user does not exist
    public UserEntity GetUserById(Guid userId)
    {
    }
    
    // Returns a New User is user does not exist
    public UserEntity GetNewOrExistingUserById(Guid userId)
    {
    }
