    public bool MyFunction()
    {
        bool result = false;
        if (someCondition = true)
        {
            // Do some processing
            result = true;
        }
        else if (someOtherCondition = true)
        {
           // Do some processing
           result = true;
        }
        // ... keep going
    
        return result;
    }
