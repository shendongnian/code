    void OnMenuItemClick(object sender, RoutedEventArgs e)
    {
        if ((sender as MenuItem).Header == "Add")
        {
            // Add an item.
        }
        if ((sender as MenuItem).Header == "Delete")
        {
            // Delete an item.
        }
    }
