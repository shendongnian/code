    static int GetIndex(IList<Item> list, Item value)
    {
        for (int index = 0; index < list.Count; index++)
        {
    	    if (list[i] == value)
    	    {
    	         return i;
    	    } 
        }
        return -1;
    }
