    DateTime start = new DateTime(2009, 12, 9, 10, 0, 0)); //10 o'clock
    DateTime end = new DateTime(2009, 13, 9, 12, 0, 0)); //20 o'clock
    DateTime now = DateTime.Now;
    
    if ((now > start) && (now < end))
    {
       //match found
    }
             
